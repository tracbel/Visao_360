/**
 * Placeholder das telas ainda não portadas. Some conforme cada tela é
 * reconstruída a partir de `docs/prototipo/01-ESPEC-UI.md` e validada contra a
 * captura correspondente em `docs/prototipo/capturas-referencia/`.
 */
export function TelaEmConstrucao() {
  return (
    <div className="card" style={{ padding: 24 }}>
      <h2 style={{ fontSize: 18, marginBottom: 8 }}>Tela em construção</h2>
      <p style={{ color: 'var(--text-secondary)' }}>
        Esta tela ainda não foi portada do protótipo de referência.
      </p>
    </div>
  );
}
